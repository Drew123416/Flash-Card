# Flash-Card
This program creates flash cards.  You can use it for shool or whatever else you might want to do.
 It outputs the files in ".docx" format, so this will work a lot better on Windows computers
